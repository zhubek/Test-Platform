// Dashboard tab — logic intentionally removed; to be rebuilt from scratch.
// Keep this as an empty placeholder so the editor tab still mounts.

export function DashboardTab() {
  return (
    <div className="flex min-h-[40vh] items-center justify-center rounded-xl border border-dashed text-sm text-muted-foreground">
      Dashboard — to be built
    </div>
  );
}
